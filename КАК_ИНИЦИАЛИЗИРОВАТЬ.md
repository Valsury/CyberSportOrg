# 🚀 Как инициализировать базу данных

## Проблема
Ошибка: `The table 'public.users' does not exist`

## Решение: Вызовите API endpoint

### Шаг 1: Получите секретный ключ

1. Зайдите на https://render.com
2. Откройте ваш **Web Service**
3. Перейдите в **"Environment"**
4. Найдите `INIT_DB_SECRET` и **скопируйте значение**

### Шаг 2: Вызовите endpoint

#### Вариант 1: Через онлайн сервис (САМЫЙ ПРОСТОЙ) ⭐

1. Откройте https://reqbin.com
2. Выберите метод: **POST**
3. URL: `https://ваш-сайт.onrender.com/api/admin/init-db-full`
   - Замените `ваш-сайт` на имя вашего сервиса
4. В разделе **Headers** добавьте:
   - **Name**: `Authorization`
   - **Value**: `Bearer ВАШ_СЕКРЕТНЫЙ_КЛЮЧ`
   - Замените `ВАШ_СЕКРЕТНЫЙ_КЛЮЧ` на значение из шага 1
5. Нажмите **Send**

#### Вариант 2: Через curl (если есть терминал)

```bash
curl -X POST https://ваш-сайт.onrender.com/api/admin/init-db-full \
  -H "Authorization: Bearer ВАШ_СЕКРЕТНЫЙ_КЛЮЧ" \
  -H "Content-Type: application/json"
```

### Шаг 3: Проверьте результат

После вызова вы должны получить ответ:

```json
{
  "success": true,
  "message": "Database fully initialized",
  "results": [
    "📦 Creating database tables...",
    "✅ Created Role enum",
    "✅ Created TeamStatus enum",
    "✅ Created users table",
    "✅ Created accounts table",
    "✅ Created sessions table",
    "✅ All tables created successfully",
    "✅ Admin user created: admin@cybersport.org",
    "🔑 Password: [ваш пароль]"
  ]
}
```

### Шаг 4: Войдите в систему

1. Обновите страницу приложения
2. Войдите используя:
   - **Email**: `admin@cybersport.org`
   - **Пароль**: тот, что указан в переменной `ADMIN_PASSWORD` на Render

## ✅ Готово!

После этого база данных будет инициализирована и вход должен работать!

## ❓ Проблемы?

### Ошибка 401 Unauthorized
- Проверьте, что заголовок `Authorization` правильно установлен
- Формат должен быть: `Bearer ВАШ_КЛЮЧ` (с пробелом после Bearer)
- Убедитесь, что ключ совпадает с `INIT_DB_SECRET` на Render

### Ошибка 500
- Проверьте логи на Render
- Убедитесь, что база данных доступна
- Проверьте переменную `DATABASE_URL`

### Таблицы все еще не создаются
- Убедитесь, что вы используете `/api/admin/init-db-full` (не `/api/admin/init-db`)
- Проверьте, что код задеплоен на Render (после изменений нужно подождать деплой)

