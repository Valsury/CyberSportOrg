# Чеклист для деплоя на Render

## ✅ Перед деплоем

- [x] Схема Prisma настроена на PostgreSQL
- [x] .env файл добавлен в .gitignore
- [x] База данных SQLite (dev.db) добавлена в .gitignore
- [x] Все зависимости установлены
- [x] Скрипты для инициализации БД готовы
- [x] Документация по деплою создана

## 📋 Шаги для деплоя

### Шаг 1: Создание PostgreSQL базы данных на Render
1. Зайдите на https://render.com
2. Нажмите "New +" → "PostgreSQL"
3. Заполните форму:
   - Name: `cybersportorg-db`
   - Region: выберите ближайший
   - Plan: Free (для тестирования)
4. Сохраните **Internal Database URL**

### Шаг 2: Подготовка репозитория
```bash
# Убедитесь, что код в Git
git status

# Если нужно, добавьте файлы
git add .
git commit -m "Prepare for Render deployment"

# Запушьте в GitHub
git push
```

### Шаг 3: Создание Web Service
1. На Render нажмите "New +" → "Web Service"
2. Подключите ваш GitHub репозиторий
3. Настройте:
   - Name: `cybersportorg`
   - Build Command: `npm install && npm run build`
   - Start Command: `npm start`
   - Plan: Free

### Шаг 4: Настройка переменных окружения
Добавьте в Render:
- `DATABASE_URL` - Internal Database URL из шага 1
- `NEXTAUTH_URL` - `https://your-app-name.onrender.com`
- `NEXTAUTH_SECRET` - сгенерируйте с `openssl rand -base64 32`
- `ADMIN_EMAIL` - `admin@cybersport.org`
- `ADMIN_PASSWORD` - надежный пароль

### Шаг 5: Инициализация базы данных
После деплоя откройте Shell на Render и выполните:
```bash
npx prisma db push
npm run db:seed
```

## 🔍 Проверка после деплоя

- [ ] Сайт открывается
- [ ] Можно войти как администратор
- [ ] Админ-панель доступна
- [ ] Можно создавать пользователей
- [ ] Нет ошибок в логах

## 📝 Важные заметки

- ⚠️ Используйте **Internal Database URL**, а не External
- ⚠️ Измените `ADMIN_PASSWORD` на надежный пароль
- ⚠️ Free план может "засыпать" после неактивности
- ⚠️ Первый деплой может занять 5-10 минут

