# Инициализация базы данных через API Endpoint

## Проблема
Shell на Render платный, поэтому нужно использовать API endpoint для инициализации базы данных.

## Решение

Созданы два защищенных endpoint'а для инициализации:

### 1. Полная инициализация (рекомендуется)
**Endpoint:** `POST /api/admin/init-db-full`

Создает все таблицы и администратора.

### 2. Только создание администратора
**Endpoint:** `POST /api/admin/init-db`

Создает только администратора (если таблицы уже существуют).

## Как использовать

### Шаг 1: Получите секретный ключ

1. Зайдите на Render Dashboard
2. Откройте ваш Web Service
3. Перейдите в раздел **"Environment"**
4. Найдите переменную `INIT_DB_SECRET`
5. Скопируйте значение (или создайте новый, если его нет)

Если переменной нет, создайте её:
- **Key**: `INIT_DB_SECRET`
- **Value**: сгенерируйте случайную строку (например, через `openssl rand -base64 32`)
- Нажмите **"Save Changes"**

### Шаг 2: Вызовите endpoint

Используйте один из способов:

#### Способ 1: Через curl (из терминала)

```bash
curl -X POST https://your-app-name.onrender.com/api/admin/init-db-full \
  -H "Authorization: Bearer YOUR_INIT_DB_SECRET" \
  -H "Content-Type: application/json"
```

Замените:
- `your-app-name.onrender.com` на URL вашего приложения
- `YOUR_INIT_DB_SECRET` на значение из переменной окружения

#### Способ 2: Через браузер (с расширением)

1. Установите расширение для браузера (например, "REST Client" или используйте Postman)
2. Создайте POST запрос на: `https://your-app-name.onrender.com/api/admin/init-db-full`
3. Добавьте заголовок:
   - **Key**: `Authorization`
   - **Value**: `Bearer YOUR_INIT_DB_SECRET`

#### Способ 3: Через Postman или Insomnia

1. Создайте новый POST запрос
2. URL: `https://your-app-name.onrender.com/api/admin/init-db-full`
3. В разделе Headers добавьте:
   - **Key**: `Authorization`
   - **Value**: `Bearer YOUR_INIT_DB_SECRET`
4. Отправьте запрос

#### Способ 4: Через онлайн сервис

Используйте https://reqbin.com или подобный сервис:
1. Выберите метод: **POST**
2. URL: `https://your-app-name.onrender.com/api/admin/init-db-full`
3. В Headers добавьте:
   ```
   Authorization: Bearer YOUR_INIT_DB_SECRET
   ```
4. Нажмите Send

### Шаг 3: Проверьте результат

После успешного вызова вы получите JSON ответ:

```json
{
  "success": true,
  "message": "Database fully initialized",
  "results": [
    "📦 Creating database tables...",
    "✅ All tables created successfully",
    "✅ Admin user created: admin@cybersport.org",
    "🔑 Password: [ваш пароль]"
  ]
}
```

### Шаг 4: Войдите в систему

После инициализации:
1. Откройте ваше приложение
2. Войдите используя:
   - **Email**: `admin@cybersport.org` (или из `ADMIN_EMAIL`)
   - **Пароль**: тот, что указан в переменной `ADMIN_PASSWORD`

## Безопасность

⚠️ **Важно:**
- Endpoint защищен секретным ключом через заголовок `Authorization`
- Используйте сильный секретный ключ
- После инициализации можно удалить переменную `INIT_DB_SECRET` (или оставить для будущих инициализаций)
- Endpoint проверяет, что таблицы/пользователи не существуют перед созданием

## Решение проблем

### Ошибка 401 Unauthorized
- Проверьте, что заголовок `Authorization` правильно установлен
- Убедитесь, что значение `INIT_DB_SECRET` совпадает в переменных окружения и в запросе
- Формат должен быть: `Bearer YOUR_SECRET_KEY`

### Ошибка 500 Internal Server Error
- Проверьте логи в Render Dashboard
- Убедитесь, что база данных доступна
- Проверьте, что переменная `DATABASE_URL` правильно настроена

### Таблицы уже существуют
- Endpoint автоматически пропустит создание существующих таблиц
- Администратор будет создан только если его еще нет

## Примеры использования

### Полная инициализация (создание таблиц + администратора)
```bash
curl -X POST https://cybersportorg.onrender.com/api/admin/init-db-full \
  -H "Authorization: Bearer my-secret-key-12345" \
  -H "Content-Type: application/json"
```

### Только создание администратора (если таблицы уже есть)
```bash
curl -X POST https://cybersportorg.onrender.com/api/admin/init-db \
  -H "Authorization: Bearer my-secret-key-12345" \
  -H "Content-Type: application/json"
```

## После инициализации

1. ✅ База данных инициализирована
2. ✅ Все таблицы созданы
3. ✅ Администратор создан
4. ✅ Можно войти в систему

**Примечание:** После успешной инициализации endpoint можно вызвать снова - он безопасно пропустит уже существующие элементы.

