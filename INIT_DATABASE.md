# Инициализация базы данных на Render

## Проблема
Если вы видите ошибку:
```
The table `public.users` does not exist in the current database.
```

Это означает, что база данных не была инициализирована.

## Решение: Инициализация через Render Shell

### Шаг 1: Откройте Shell на Render

1. Зайдите на https://render.com
2. Откройте ваш **Web Service** (не базу данных!)
3. Перейдите в раздел **"Shell"** (или найдите кнопку "Open Shell")
4. Нажмите **"Connect"** или **"Open Shell"**

### Шаг 2: Выполните команды инициализации

В открывшемся Shell выполните команды **по порядку**:

```bash
# 1. Создать все таблицы в базе данных
npx prisma db push

# 2. Создать администратора
npm run db:seed
```

**Важно:** Выполняйте команды по очереди, дождитесь завершения первой перед выполнением второй.

## Ожидаемый результат

После выполнения вы должны увидеть:

```
🚀 Starting database initialization...
📦 Pushing Prisma schema to database...
✅ Schema pushed successfully
👤 Creating admin user: admin@cybersport.org
✅ Admin user created: admin@cybersport.org
🔑 Password: [ваш пароль]
✅ Database initialization completed!
```

## После инициализации

1. **Обновите страницу** вашего приложения
2. **Попробуйте войти** используя:
   - Email: `admin@cybersport.org` (или из `ADMIN_EMAIL`)
   - Пароль: тот, что указан в переменной `ADMIN_PASSWORD`

## Проверка

После инициализации проверьте:
- ✅ Нет ошибок в логах Render
- ✅ Можно войти в систему
- ✅ Админ-панель доступна

## Решение проблем

### Ошибка "command not found"
- Убедитесь, что вы в Shell Web Service, а не базы данных
- Попробуйте использовать полный путь: `npx tsx src/scripts/init-production.ts`

### Ошибка подключения к БД
- Проверьте, что переменная `DATABASE_URL` правильно настроена
- Убедитесь, что база данных запущена
- Проверьте, что используется Internal Database URL

### Ошибка "permission denied"
- Убедитесь, что вы используете Shell Web Service
- Попробуйте выполнить команды по отдельности

## Автоматическая инициализация (будущее улучшение)

В будущем можно добавить автоматическую инициализацию при первом запуске приложения, но пока нужно выполнить вручную через Shell.

