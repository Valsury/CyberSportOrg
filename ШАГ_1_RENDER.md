# ШАГ 1: Создание PostgreSQL базы данных на Render

## Что нужно сделать:

### 1. Зайдите на Render
- Откройте https://render.com
- Войдите в аккаунт (или создайте новый)

### 2. Создайте PostgreSQL базу данных
1. Нажмите кнопку **"New +"** (в правом верхнем углу)
2. Выберите **"PostgreSQL"**

### 3. Заполните форму создания БД:
- **Name**: `cybersportorg-db` (или любое другое имя)
- **Database**: `cybersportorg` (или оставьте значение по умолчанию)
- **User**: оставьте значение по умолчанию
- **Region**: выберите ближайший регион
  - Например: `Frankfurt (EU)` для Европы
  - Или `Oregon (US West)` для США
- **PostgreSQL Version**: выберите последнюю версию (обычно `16`)
- **Plan**: выберите **Free** (для тестирования)

### 4. Нажмите **"Create Database"**

### 5. Сохраните Internal Database URL
После создания базы данных:
1. Откройте настройки созданной базы данных
2. Найдите раздел **"Connections"**
3. Скопируйте **Internal Database URL**
   - Он выглядит примерно так: `postgresql://user:password@dpg-xxxxx-a.region/database`
   - ⚠️ **ВАЖНО**: Используйте **Internal Database URL**, а не External!
   - Internal URL работает только внутри сети Render и быстрее

### 6. Сохраните URL в безопасном месте
Этот URL понадобится на следующем шаге для настройки Web Service.

---

## ✅ Готово!

После выполнения этого шага у вас будет:
- ✅ PostgreSQL база данных на Render
- ✅ Internal Database URL для подключения

## 📝 Следующий шаг:

После создания базы данных переходите к **ШАГ 2**: Создание Web Service на Render.

---

## 💡 Полезные советы:

- **Free план** подходит для тестирования, но имеет ограничения
- **Internal Database URL** безопаснее и быстрее, чем External
- Сохраните URL в безопасном месте - он понадобится позже
- База данных на Free плане может "засыпать" после неактивности (это нормально)

## ❓ Возникли проблемы?

- Убедитесь, что вы авторизованы на Render
- Проверьте, что выбрали правильный регион
- Если что-то не получается - проверьте логи в Render Dashboard

