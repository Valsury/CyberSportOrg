# Деплой через Render Blueprint

## Что такое Blueprint?

Blueprint позволяет автоматически создать все необходимые сервисы (база данных, web service) из одного YAML файла. Это намного проще, чем создавать все вручную!

## Шаг 1: Подготовка репозитория

### 1.1. Убедитесь, что код в Git
```bash
git status
```

### 1.2. Если репозитория нет, создайте его
```bash
git init
git add .
git commit -m "Initial commit - ready for Render deployment"
```

### 1.3. Создайте репозиторий на GitHub
1. Зайдите на https://github.com
2. Создайте новый репозиторий (например, `CyberSportOrg`)
3. Не добавляйте README, .gitignore или лицензию (они уже есть)

### 1.4. Подключите локальный репозиторий к GitHub
```bash
git remote add origin https://github.com/YOUR_USERNAME/CyberSportOrg.git
git branch -M main
git push -u origin main
```

## Шаг 2: Деплой через Blueprint на Render

### 2.1. Зайдите на Render
1. Откройте https://render.com
2. Войдите в аккаунт (или создайте новый)

### 2.2. Создайте новый Blueprint
1. Нажмите кнопку **"New +"** в правом верхнем углу
2. Выберите **"Blueprint"**
3. Выберите **"Connect GitHub"** (или GitLab, если используете его)
4. Авторизуйте Render для доступа к вашим репозиториям

### 2.3. Выберите репозиторий
1. Выберите ваш репозиторий `CyberSportOrg`
2. Render автоматически обнаружит файл `render.yaml`
3. Нажмите **"Apply"**

### 2.4. Настройте переменные окружения
Render покажет форму, где нужно указать:

1. **NEXTAUTH_URL**
   - Значение: `https://cybersportorg.onrender.com`
   - Или оставьте пустым - Render автоматически установит после деплоя
   - ⚠️ После первого деплоя вернитесь и обновите на правильный URL

2. **ADMIN_PASSWORD**
   - Значение: придумайте надежный пароль
   - ⚠️ **НЕ ИСПОЛЬЗУЙТЕ** `admin123` в production!
   - Пример: `MySecurePass123!@#`

3. **DATABASE_URL** - автоматически заполнится из базы данных
4. **NEXTAUTH_SECRET** - автоматически сгенерируется

### 2.5. Выберите регион
- Выберите ближайший регион (например, `Frankfurt (EU)` или `Oregon (US West)`)
- Убедитесь, что и база данных, и web service в одном регионе

### 2.6. Запустите деплой
1. Нажмите **"Apply"** или **"Create Resources"**
2. Render начнет создавать:
   - PostgreSQL базу данных
   - Web Service
   - Все необходимые связи

## Шаг 3: Ожидание деплоя

### 3.1. Дождитесь завершения
- Создание базы данных: ~2-3 минуты
- Сборка и деплой web service: ~5-10 минут
- Вы можете следить за прогрессом в Dashboard

### 3.2. Проверьте статус
- Зеленый индикатор = готово
- Желтый/красный = есть проблемы (проверьте логи)

## Шаг 4: Инициализация базы данных

### 4.1. Откройте Shell на Render
После успешного деплоя:
1. Откройте ваш Web Service на Render
2. Перейдите в раздел **"Shell"** (или используйте **"Logs"**)
3. Откройте Shell

### 4.2. Выполните команды инициализации
В Shell выполните:

```bash
# Применить схему базы данных
npx prisma db push

# Создать администратора
npm run db:seed
```

### 4.3. Проверьте результат
Вы должны увидеть:
```
✅ Admin user created: admin@cybersport.org
🔑 Password: [ваш пароль из ADMIN_PASSWORD]
```

## Шаг 5: Обновление NEXTAUTH_URL

### 5.1. Узнайте URL вашего приложения
1. Откройте ваш Web Service на Render
2. Найдите URL в разделе "Settings" → "Service Details"
3. URL будет примерно таким: `https://cybersportorg.onrender.com`

### 5.2. Обновите переменную окружения
1. Откройте ваш Web Service
2. Перейдите в **"Environment"**
3. Найдите переменную `NEXTAUTH_URL`
4. Обновите на правильный URL: `https://cybersportorg.onrender.com`
5. Нажмите **"Save Changes"**
6. Render автоматически перезапустит сервис

## Шаг 6: Проверка

### 6.1. Откройте сайт
1. Откройте URL вашего приложения
2. Вы должны увидеть страницу входа

### 6.2. Войдите в систему
Используйте:
- **Email**: `admin@cybersport.org` (или тот, который указали в `ADMIN_EMAIL`)
- **Пароль**: тот, который указали в `ADMIN_PASSWORD`

### 6.3. Проверьте функционал
- [ ] Вход работает
- [ ] Дашборд открывается
- [ ] Админ-панель доступна
- [ ] Можно создавать пользователей
- [ ] Нет ошибок в логах

## Преимущества Blueprint

✅ Автоматическое создание всех сервисов  
✅ Автоматическая настройка связей между сервисами  
✅ Один файл для управления всей инфраструктурой  
✅ Легко обновлять и передеплоить  
✅ Версионирование конфигурации в Git  

## Решение проблем

### Ошибка при создании Blueprint
- Убедитесь, что файл `render.yaml` находится в корне репозитория
- Проверьте синтаксис YAML файла
- Убедитесь, что репозиторий подключен к Render

### Ошибка подключения к БД
- Убедитесь, что база данных создана
- Проверьте, что `DATABASE_URL` правильно настроен
- Убедитесь, что оба сервиса в одном регионе

### Ошибки при сборке
- Проверьте логи сборки в Render
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте версию Node.js

### Проблемы с аутентификацией
- Убедитесь, что `NEXTAUTH_URL` указывает на правильный URL
- Проверьте, что `NEXTAUTH_SECRET` установлен
- Убедитесь, что база данных инициализирована (`npx prisma db push`)

## Обновление после изменений

После изменений в коде:
1. Сделайте commit и push в GitHub
2. Render автоматически обнаружит изменения
3. Начнется новый деплой
4. Дождитесь завершения

## Удаление сервисов

Если нужно удалить все:
1. Откройте Dashboard на Render
2. Удалите Web Service
3. Удалите базу данных
4. Или используйте команду в Shell (если доступна)

## Следующие шаги

После успешного деплоя:
- Настройте домен (опционально)
- Настройте автоматические бэкапы базы данных
- Настройте мониторинг
- Рассмотрите upgrade на платный план для production

