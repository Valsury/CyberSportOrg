# 📞 Как вызвать endpoint для инициализации БД

## Быстрая инструкция

### Шаг 1: Убедитесь, что код задеплоен

Если вы только что внесли изменения, нужно:
```bash
git add .
git commit -m "Add init-db endpoint"
git push
```

Дождитесь завершения деплоя на Render (5-10 минут).

### Шаг 2: Получите секретный ключ

1. Откройте https://render.com
2. Ваш Web Service → **Environment**
3. Найдите `INIT_DB_SECRET` и скопируйте значение

### Шаг 3: Вызовите endpoint

#### Способ 1: Через ReqBin (РЕКОМЕНДУЕТСЯ) ⭐

1. Откройте https://reqbin.com
2. Метод: **POST**
3. URL: `https://cybersportorg.onrender.com/api/admin/init-db-full`
   - Замените `cybersportorg` на имя вашего сервиса
4. Вкладка **Headers**:
   - **Name**: `Authorization`
   - **Value**: `Bearer ВАШ_СЕКРЕТНЫЙ_КЛЮЧ`
5. Нажмите **Send**

#### Способ 2: Через браузер (с расширением)

Установите расширение "REST Client" или используйте встроенный инструмент разработчика браузера.

#### Способ 3: Через curl

```bash
curl -X POST https://cybersportorg.onrender.com/api/admin/init-db-full \
  -H "Authorization: Bearer ВАШ_СЕКРЕТНЫЙ_КЛЮЧ"
```

### Шаг 4: Проверьте ответ

Должен быть JSON с `"success": true` и списком созданных таблиц.

### Шаг 5: Войдите в систему

После успешной инициализации:
- Email: `admin@cybersport.org`
- Пароль: из переменной `ADMIN_PASSWORD` на Render

## Пример полного запроса

**URL:**
```
POST https://cybersportorg.onrender.com/api/admin/init-db-full
```

**Headers:**
```
Authorization: Bearer abc123xyz789
Content-Type: application/json
```

**Body:** (пустой, не нужен)

## Ожидаемый ответ

```json
{
  "success": true,
  "message": "Database fully initialized",
  "results": [
    "📦 Creating database tables...",
    "✅ Created Role enum",
    "✅ Created users table",
    "✅ Created accounts table",
    "✅ All tables created successfully",
    "✅ Admin user created: admin@cybersport.org",
    "🔑 Password: [ваш пароль]"
  ]
}
```

## Если не работает

1. Проверьте, что код задеплоен (посмотрите последний деплой на Render)
2. Проверьте логи на Render - там будут ошибки, если что-то не так
3. Убедитесь, что используете правильный URL вашего приложения
4. Проверьте, что секретный ключ правильный

