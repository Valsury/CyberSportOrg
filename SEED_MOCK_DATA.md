# Загрузка моковых данных через API

## Описание

API endpoint для загрузки моковых данных на Render (или локально). Создает:
- 8 игроков с аватарами
- 2 менеджера
- 2 команды (CS2 и Dota 2) с игроками
- 4 турнира

## Endpoint

**URL:** `POST /api/admin/seed-mock-data`

**Требования:**
- Авторизация: только ADMIN
- Метод: POST

## Как использовать

### Способ 1: Через браузер (с расширением REST Client)

1. Установите расширение REST Client для браузера (например, Thunder Client для VS Code)
2. Войдите в систему как администратор
3. Создайте POST запрос:
   ```
   POST https://your-app-name.onrender.com/api/admin/seed-mock-data
   ```

### Способ 2: Через curl (из терминала)

```bash
# Сначала получите сессионную cookie (войдите через браузер и скопируйте cookie)
# Или используйте API для входа и получите токен

curl -X POST https://your-app-name.onrender.com/api/admin/seed-mock-data \
  -H "Content-Type: application/json" \
  -H "Cookie: next-auth.session-token=YOUR_SESSION_TOKEN"
```

### Способ 3: Через Postman/Insomnia

1. Создайте POST запрос на `https://your-app-name.onrender.com/api/admin/seed-mock-data`
2. Добавьте заголовок `Content-Type: application/json`
3. Добавьте cookie сессии (скопируйте из браузера после входа)

### Способ 4: Через браузер (JavaScript Console)

Откройте консоль браузера на странице приложения (после входа как админ) и выполните:

```javascript
fetch('/api/admin/seed-mock-data', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
})
  .then(res => res.json())
  .then(data => {
    console.log('Результат:', data);
    alert('Моковые данные загружены! Проверьте консоль для деталей.');
  })
  .catch(error => {
    console.error('Ошибка:', error);
    alert('Ошибка при загрузке данных');
  });
```

## Ответ

### Успешный ответ (200)

```json
{
  "success": true,
  "message": "Mock data seeded successfully",
  "results": [
    "📝 Создание игроков...",
    "  ✅ Создан игрок: Алексей 'S1mple' Костилев (player1@afina.org)",
    "...",
    "📊 Статистика:",
    "  - Игроков: 8",
    "  - Менеджеров: 2",
    "  - Команд: 2",
    "  - Турниров: 4 новых"
  ],
  "stats": {
    "players": 8,
    "managers": 2,
    "teams": 2,
    "tournaments": 4
  }
}
```

### Ошибка авторизации (401)

```json
{
  "error": "Unauthorized. Admin access required."
}
```

### Ошибка сервера (500)

```json
{
  "success": false,
  "error": "Error message",
  "details": "Error code"
}
```

## Учетные данные для тестирования

После загрузки моковых данных вы можете войти:

**Игроки:**
- Email: `player1@afina.org` - `player8@afina.org`
- Пароль: `player123`

**Менеджеры:**
- Email: `manager1@afina.org`, `manager2@afina.org`
- Пароль: `manager123`

## Примечания

- Скрипт проверяет существование данных перед созданием, поэтому его можно запускать несколько раз
- Если данные уже существуют, они не будут перезаписаны
- Аватары генерируются через DiceBear API
- Все пароли хешируются с помощью bcryptjs

## Локальное использование

Для локального использования можно также запустить скрипт напрямую:

```bash
npm run db:seed:mock
```

Но для Render рекомендуется использовать API endpoint, так как Shell платный.

